server {
    listen 443 ssl;
    listen [::]:443 ssl;

    server_name mti-app 10.100.0.118;
    #ssl_certificate /home/ssl_https/cert.pem;
    #ssl_certificate_key /home/ssl_https/key.pem;
    ssl_certificate /home/ssl_mkcert/cert.pem;
    ssl_certificate_key /home/ssl_mkcert/key.pem;

    location / {
        proxy_pass http://mti-app:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_redirect off;
    }
    location /static/ {
        autoindex on;
        alias /app/staticfiles/;
    }

    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log warn;
}
